# Continuum — Python Dependencies
# ---------------------------------
# Top-level pinned requirements for hackathon development.
# For production use, pin to exact versions via pip-compile or Poetry.
#
# Install: pip install -r requirements.txt

# ── Realtime transport ────────────────────────────────────────────────────────
livekit-agents>=0.11,<1.0
livekit-plugins-rime>=0.3,<1.0          # Official Rime plugin for LiveKit Agents
livekit-plugins-deepgram>=0.6,<1.0      # STT: Deepgram streaming (primary)
livekit-plugins-silero>=0.6,<1.0        # VAD (Voice Activity Detection)

# ── Data validation (schemas) ─────────────────────────────────────────────────
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0

# ── Web framework (mock freshness API + control surface) ─────────────────────
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy[asyncio]>=2.0,<3.0
aiosqlite>=0.20,<1.0                    # SQLite async driver (hackathon default)
# asyncpg>=0.29,<1.0                    # Uncomment to switch to Postgres

# ── LLM / AI ──────────────────────────────────────────────────────────────────
google-generativeai>=0.7,<1.0           # Gemini models
anthropic>=0.30,<1.0                    # Claude models (optional)

# ── HTTP client (Rime API calls, freshness checks) ────────────────────────────
httpx>=0.27,<1.0
aiohttp>=3.10,<4.0

# ── Environment ───────────────────────────────────────────────────────────────
python-dotenv>=1.0,<2.0

# ── Evaluation & testing ──────────────────────────────────────────────────────
pytest>=8.0,<9.0
pytest-asyncio>=0.24,<1.0

# ── Audio utilities (evaluation harness) ─────────────────────────────────────
soundfile>=0.12,<1.0                    # WAV/MP3 read/write for evidence clips
numpy>=1.26,<3.0                        # Audio array operations
